// Placeholder para futura integración con Google OAuth
function initAuth() {
    // Aquí en el futuro: botones de "Iniciar sesión con Google"
    // y llamadas al backend.
}

window.initAuth = initAuth;
